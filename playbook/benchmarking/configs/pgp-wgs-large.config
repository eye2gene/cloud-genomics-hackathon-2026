// Custom config for PGP-UK whole genome FASTQs (40-50 GB per file)
// The default sarek memory for FASTP/FASTQC (4 GB) is too low for
// the AWS CLI multipart S3 download of files this large.
// See: https://github.com/aws/aws-cli/issues/2390

// Sentieon licence - set path to your licence file
env.SENTIEON_LICENSE = '/tmp/sentieon.lic'

process {
    queue = '<YOUR-SPOT-QUEUE>'

    // Retry on OOM, spot interruption, and transient failures
    errorStrategy = { task.exitStatus in [1, 137, 143, 255] ? 'retry' : 'finish' }
    maxRetries = 3

    withName: 'FASTQC' {
        cpus = { 4 * task.attempt }
        memory = { 64.GB * task.attempt }
    }
    withName: 'FASTP' {
        cpus = { 12 * task.attempt }
        memory = { 64.GB * task.attempt }
    }
    withName: 'GATK4_BASERECALIBRATOR' {
        memory = { 64.GB * task.attempt }
    }
    withName: 'MOSDEPTH' {
        memory = { 32.GB * task.attempt }
    }
    withName: 'SAMTOOLS_STATS' {
        memory = { 16.GB * task.attempt }
    }
    withName: 'SENTIEON.*' {
        beforeScript = '/opt/aws-cli/bin/aws s3 cp s3://<YOUR-BUCKET>/configs/sentieon.lic /tmp/sentieon.lic 2>/dev/null || true'
    }
    withName: 'BWAMEM2_MEM' {
        cpus = 24
        memory = { 64.GB * task.attempt }
    }
}

report {
    overwrite = true
}
timeline {
    overwrite = true
}
tower {
    enabled = true
    // Set TOWER_ACCESS_TOKEN env var at submit time
}
