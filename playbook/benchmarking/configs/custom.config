/*
 * Custom resource overrides for nf-core/sarek on AWS Batch
 * Tuned for cost efficiency without sacrificing performance.
 */
process {
    withName: 'FASTQC' {
        cpus   = 2
        memory = 2.GB
    }
}
